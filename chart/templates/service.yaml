apiVersion: v1
kind: Service
metadata:
  name: {{ include "lilurl.fullname" . }}
  labels:
    {{- include "lilurl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lilurl.selectorLabels" . | nindent 4 }}
