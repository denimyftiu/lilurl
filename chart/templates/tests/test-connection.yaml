apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lilurl.fullname" . }}-test-connection"
  labels:
    {{- include "lilurl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lilurl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
